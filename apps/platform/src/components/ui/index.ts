/**
 * UI Components Index
 *
 * Re-export all UI components. These extend the shared @paycore/ui
 * components with platform-specific features and animations.
 */

export * from './animated';
export { AnimatedCounter, AnimatedCurrency } from './animated-counter';
export { StatCard, MiniStat } from './stat-card';
export { ProgressRing, ProgressBar } from './progress-ring';
export { PageHeader, PageContainer, Section } from './page-header';
